mqtt:
  enabled: false

cameras:
  name_of_your_camera: # <------ Name the camera
    enabled: true
    ffmpeg:
      inputs:
        - path: "rtsp://10.0.0.136:8554/live"
          roles:
            - detect   # Para detección de objetos
            - rtmp
    detect:
      enabled: false # <---- disable detection until you have a working camera feed
      width: 1280
      height: 720
detect:
  enabled: true
version: 0.16-0
